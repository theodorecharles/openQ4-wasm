# Phase H recon — MSAA slice + H/I boundary (ground truth)

## 1. MSAA in the GL backend

**Cvars / normalization**
- `r_multiSamples` default **"0"**, CVAR_ARCHIVE|INTEGER, range 0–16, discrete completion {0,2,4,8,16} — `src\renderer\RenderSystem_init.cpp:212,217`. Normalized to those steps pre-GL-init by `R_NormalizeMultiSamplesValue` (`RenderSystem_init.cpp:1504-1518`) from `R_NormalizeDisplayCvars` (`:1561-1567`). Token-pinned by `tools\tests\renderer_msaa_cvar_safety.py` (asserts exact source strings in RenderSystem_init.cpp and the openQ4-game companion — keep wording intact or update the test).
- `r_msaaAlphaToCoverage` default "1" (`RenderSystem_init.cpp:271`); `r_msaaResolveDepth` default "0" (`:270`, sole reader is game `openQ4-game\src\game\Game_render.cpp:1264`).

**Context creation (window-surface MSAA)** — `parms.multiSamples = r_multiSamples` (`RenderSystem_init.cpp:1633`; safe-mode retry resets to 0 at `:1651`). SDL3 ladder tries each context candidate × sample-fallback list, then writes the achieved count back into `r_multiSamples` (`src\renderer\OpenGL\gl_ContextSDL3.cpp:354-431`, write-back `:425-429`). This only affects the default framebuffer; the real scene MSAA is FBO-based.

**Two independent MSAA render-target consumers**
1. Engine FP16 scene target: `RB_EnsureSceneRenderTexture` (`draw_common.cpp:1603-1662`) allocates `_hdrSceneColor` RGBA16F + `_hdrSceneDepthStencil` with `numMSAASamples = r_multiSamples`, but only when **not** supersampling and **not** modern-visible post (`:1613-1616`). MSAA scene color can't be presented directly (`canPresentSceneColorDirectly` requires ≤1 sample, `draw_common.cpp:3781-3785`) so present resolves via `RB_CaptureCurrentRenderImage`/`CopyFramebuffer` blit.
2. Game openQ4 post chain: `openQ4_CreateForwardRenderTarget` (`openQ4-game\src\game\Game_render.cpp:933-1002`) allocates `_forwardRenderAlbedo` RGBA8 + `_forwardRenderDepth` at `r_multiSamples` with a fallback ladder (`openQ4_NextLowerMSAASampleCount` `:920-931`) that depends on `GetImageMSAASamples` (`RenderSystem.cpp:2090-2092`, returns `opts.numMSAASamples`) reflecting the **allocated** count — the GL path clamps `opts.numMSAASamples` in place (`OpenGL\gl_Image.cpp:493-507`). Then `renderSystem->ResolveMSAA(forwardRenderPassRT, forwardRenderPassResolvedRT, blurEnabled || r_msaaResolveDepth)` at `Game_render.cpp:1261-1264` — this is the **only** ResolveMSAA caller anywhere.

**RC_RESOLVE_MSAA** — front-end `idRenderSystemLocal::ResolveMSAA` (`RenderSystem.cpp:1864-1879`); GL executor `RB_ResolveMSAA` (`tr_backend.cpp:630-685`): per-color-attachment `glBlitFramebuffer` (COLOR, NEAREST), optional depth blit when `cmd->resolveDepth`, then restores the tracked render texture (`RB_RestoreTrackedRenderTexture` `:616-623`).

**Alpha-to-coverage** — `RB_UseAlphaToCoverage` (`draw_common.cpp:623-646`): needs cvar + MC_PERFORATED + ARB_multisample + `backEnd.renderTexture` color image with >1 samples. Applied in depth fill (`:5954`, `glEnable(GL_SAMPLE_ALPHA_TO_COVERAGE)` `:5975-5977`) and three other stage walks (`:5432`, `:6543`, `:8750`).

**Effective-MSAA gating** (`R_GfxInfoPrintAAState`, `RenderSystem_init.cpp:3422-3500`): suppressed by supersampling (screenFraction>100), modern-visible post, missing ARB_texture_multisample, and GL_MAX_SAMPLES clamp. MSAA texture alloc = `GL_TEXTURE_2D_MULTISAMPLE`/`glTexImage2DMultisample` (`gl_Image.cpp:463-507`); FBO attach `gl_RenderTexture.cpp:430-441`.

**What vk MSAA needs**
- Swapchain images are 1-sample (`Vulkan\VulkanDevice.cpp:279`, usage COLOR_ATTACHMENT|TRANSFER_DST); per-slot depth is 1-sample (`:145-153`). MSAA ⇒ offscreen multisampled color (swapchainFormat) + depth (vkCtx.depthFormat) transients, resolved via `VkRenderingAttachmentInfo::resolveImageView` (dynamic rendering already in use) or `vkCmdResolveImage`.
- Every pipeline goes through shared `VK_Exec_CreatePipeline` which hardwires `rasterizationSamples = VK_SAMPLE_COUNT_1_BIT` (`vk_GuiExecutor.cpp:332-335`, bound at `:402`); caches key **only on blendBits** (`VK_GuiExecutor_GetPipeline` `:416-427`; cube variant `:469+`; interaction/shadow pipelines in `vk_Interactions.cpp`/`vk_ShadowMap.cpp` use the same helper). Pipeline-cache implication: key must gain sample count (or flush all pipelines on sample change), plus an `alphaToCoverageEnable` bit for perforated depth-fill/ambient parity with `draw_common.cpp:5954/5975`. Shadow atlas stays 1-sample (`vk_ShadowMap.cpp:265,375`).
- `vk_Image.cpp` AllocImage **ignores** `opts.numMSAASamples` (samples hardwired at `vk_Image.cpp:377`) and does not zero it — so `GetImageMSAASamples` lies under vk: the game ladder (`Game_render.cpp:963-982`) would believe MSAA was allocated. Phase H must either honor `numMSAASamples` in vk AllocImage (color/depth-attachment usage + resolve pairing) or clamp `opts.numMSAASamples = 0` so the game path stays truthful.
- Clamp against `VkPhysicalDeviceLimits::framebufferColor/DepthSampleCounts` (analog of the GL_MAX_SAMPLES clamp `gl_Image.cpp:495-502`).

**Is MSAA pure opt-in?** Cvar default 0, yes — **but** `Com_ExecMachineSpec_f` sets r_multiSamples 8/4/2 for machineSpec 3/2/1 (`framework/Common.cpp:2280,2305,2327`; 0 only for low `:2351`), and the performance-preset system applies/verifies it (`Common.cpp:2716`, high tiers require ≥2/≥4 `:3148,:3157`). Settings menu also exposes it (`docs/dev/settings-menu-registry.json`). So preset users on high/ultra get MSAA silently dropped under vk until H lands — not a correctness break, but a visible-quality parity gap.

## 2. r_screenFraction / supersampling

- Defs: `r_screenFraction` default "100", range 10–200 (`RenderSystem_init.cpp:567`); `r_resolutionScaleMode` default "1" (0 legacy crop / 1 bilinear / 2 HQ upscale, `:568`); `r_resolutionScaleSharpness` (`:569`).
- Readers:
  - **<100, mode 0**: front-end crop in BeginFrame (`RenderSystem.cpp:1370-1379`, `CropRenderSize`) — API-agnostic, works under vk today.
  - **<100, modes 1/2**: GL swap-time fullscreen pass `RB_ApplyResolutionScaleToBackBuffer` (`draw_common.cpp:3916-4017`) called from `RB_SwapBuffers` (`tr_backend.cpp:553`), samples the back buffer on a reduced grid via GLSL (`:3969-4013`). GL-only TU — vk has no equivalent (H post family, alongside the CRT/color-mapping swap passes `RB_ApplyCRTToBackBuffer` `draw_common.cpp:4064`, `RB_ApplyColorMappingsToBackBuffer` `:4292`, both invoked `tr_backend.cpp:554-555`).
  - **>100 supersampling**: scene-target path — `RB_ComputeSupersampledSceneSize` (`draw_common.cpp:1097-1111`, clamped by GL max texture size `:1042-1077`), `RB_BeginSceneSupersampling` (`:1190-1239`, rescales viewDef viewport/scissor plus every drawSurf/light/entity scissor in place), restore `:1241-1249`, downsample happens in `RB_PresentSceneRenderTargetToBackBuffer` (`:3760-3877`). Supersampling deliberately forces the scene target to 1-sample (no MSAA stacking, `:1613-1616`). Requesting the scene target for supersampling: `RB_ViewRequestsSceneRenderTarget` (`:1310-1342`).
- Perf presets pin r_screenFraction (100 for mid+/85 for low, `Common.cpp:2282,2307,2329,2353`; preset checks `:3121-3165`).

## 3. H vs I boundary — what RB_STD_DrawView (`draw_common.cpp:9663-9853`) + GL RB_ExecuteBackEndCommands (`tr_backend.cpp:830-918`) still contain beyond E/F/G

**H (post chain + captures + MSAA + readback):**
- SS_POST_PROCESS surface pass: cut-off at `RB_STD_FindPostProcessStart` (`:7364`, walk stops `draw_common.cpp:7159-7162`), tail submission `:9833-9835`; automatic `_currentRender`/`_currentDepth` capture when first post surface arrives (`:7062-7111`) plus `MF_NEED_CURRENT_RENDER` pre-post captures (`:7146-7156`). vk already stops its ambient walks at this boundary (`vk_GuiExecutor.cpp:2244-2253`).
- Engine post chain per cvars: `RB_STD_SSAO` (`:2068`, called `:9824`) + `RB_CaptureSSAOWorldDepthImage` (`:9707`), `RB_STD_MotionBlur` (`:2725`, `:9827`), `RB_STD_Bloom` (`:3351`, `:9830`); the FP16 scene target + present (`RB_EnsureSceneRenderTexture` `:1603`, `RB_PresentSceneRenderTargetToBackBuffer` `:9839-9841`), supersampling begin/restore (`:9694`, `:9842`).
- Portal-sky preserve-far-depth machinery (all tied to the scene-target present): `RB_MarkPortalSkyBackdropForSceneTarget` `:9678`, `RB_PortalSkySceneTargetView` mark `:9684-9687`, `RB_CaptureSceneRenderTargetPreserveDepthImage` `:9821`/`:1574-1601`, depth-aware present `:3804-3872`, portal-sky skip in depth fill `:5926-5929`. Portal-sky *subview rendering* itself is already a separate RC_DRAW_VIEW the vk backend draws; the game capture callback (`SetPortalSkyCaptureViewCallback`, `RenderSystem.cpp:632`; used in screenshots `RenderSystem_init.cpp:2244-2250`) rides RenderScene + captures.
- Backend commands currently dropped by vk (`vk_Backend.cpp:382-384`): RC_SET_RENDERTEXTURE (`tr_backend.cpp:860-865`, GL impl `:600-614`; blocked on `idRenderTexture::MakeCurrent` returning false, `vk_Backend.cpp:434-436`), RC_RESOLVE_MSAA (`:866-871`), RC_CLEAR_RENDERTARGET (`:872-877`, GL impl `:692-719`), RC_SET_POSTPROCESS_SOURCE_SIZE/COLOR_SPACE/SMAA_QUALITY (`:878-886`, pure backEnd field writes — trivial), RC_COPY_RENDER (`:908-913`, GL impl `:574-592` → CopyFramebuffer/CopyDepthbuffer).
- Readback: `R_ReadTiledPixels` (`RenderSystem_init.cpp:2224-2306`) and `CaptureRenderToFile` (`RenderSystem.cpp:1722-1754`) call `glReadPixels`, which is an empty stub under vk (`vk_GLStubs.cpp:278`) — hence garbage screenshots. Swapchain lacks `VK_IMAGE_USAGE_TRANSFER_SRC_BIT` (`VulkanDevice.cpp:279`) — must be added for swapchain readback/copies. Screenshot flow holds the frame pre-present via `tr.takingScreenshot` (`tr_backend.cpp:562-564`); envshot/envcapture ride `TakeScreenshot` (`RenderSystem_init.cpp:2320-2388,3113`).
- **Demo capture: no dedicated RC commands.** `CaptureRenderToImage` writes DS_RENDER/DC_CAPTURE_RENDER into demo files at the front end (`RenderSystem.cpp:1684-1692`) and playback replays it through `RenderWorld_demo.cpp:405` → CaptureRenderToImage → RC_COPY_RENDER. H's RC_COPY_RENDER covers demos; AVI capture rides the screenshot readback.

**I (long tail):**
- Mirror/subview near-clip plane: alpha-notch texgen trick in depth fill (`draw_common.cpp:6106-6112`, filtered variant `:6243-6249`, per-surface plane `:5908-5917`; front-end `tr_subview.cpp:297` numClipPlanes=1). vk depth fill has **no clip-plane analog** — only the isMirror cull flip (`vk_GuiExecutor.cpp:2070`); needs gl_ClipDistance or shader discard. Subview viewDefs themselves already arrive as separate RC_DRAW_VIEW commands.
- isXraySubview: front-end `tr_subview.cpp:307-321,533-546`; backend touchpoints are minor — commented-out filter `draw_common.cpp:7140-7144`, fog suppression `:7766` (belongs to the G fog port), ScenePackets skip `ScenePackets.cpp:1570`.
- `RB_STD_DrawPlayerVisibilityEffects` (`:9557-9632`, called `:9781`): MP brightskin/outline/rimlight passes (GLSL rimlight + fixed-function fallback).
- `RenderPortalFades` (`:9747-9749`, r_portalsDistanceCull default 1): draws via shared front-end TU whose GL calls hit vk stubs → silently missing under vk today.
- `RB_RenderDebugTools` (`tr_rendertools.cpp:2914`, called `:9837`) and `RB_ShowOverdraw` (`tr_rendertools.cpp:485`, called `tr_render.cpp:1072`) — tr_rendertools.cpp is excluded from the vk build (`tools\build\meson_sources.py:119`).
- RC_DRAW_SPECIAL_EFFECTS / BSE RV-special effects: `RB_DrawSpecialEffects` (`draw_common.cpp:5790-5825`, executor `tr_backend.cpp:849-858`) + `RB_DisplaySpecialEffects` (`:5827+`, called `:9703,:9720`) — depth-mask captures into render textures + GLSL composites (blur/AL). Judgment: tag I, but it *depends on* H's render-texture/capture infrastructure; could be pulled into late H if RV effects matter for the soak.
- `RB_STD_ForceAmbient` (`:7895`, called `:9798`): r_forceAmbient default "0" (`RenderSystem_init.cpp:474`) and presets pin 0.0 — off-by-default parity item, I.

**Neither (no work needed):**
- Stereo: dead — `parms.stereo` is always false (`RenderSystem_init.cpp:1634,3838`; `vk_Backend.cpp:141`); no left/right buffer paths in RB_SetBuffer (`tr_backend.cpp:492-518`).
- Light scale: `RB_DetermineLightScale`/`RB_STD_LightScale` (`:9710,:9744`, `:7828`) already ported — `VK_DetermineLightScale` (`vk_Interactions.cpp:218-226`); overBright stays 1.0 under vk so the fullscreen uplight is a no-op.
- RC_SWAP_BUFFERS modern-GL compose + debug overlays (`tr_backend.cpp:894-907`) are ModernGLExecutor-only; vk stubs return false (`vk_Backend.cpp:703-724`).

## 4. RenderDoc integration
- Engine-side only: `framework\RenderDoc.cpp` loads the in-process RENDERDOC_GetAPI 1.6.0 from the injected module (`:182-219`), sets the capture path template (`:221-243`), registers `renderDocStatus`/`renderDocCapture` commands (`:325-336`; hooked in `Common.cpp:4701`, shutdown `:5635`). All of it is API-agnostic — RenderDoc's Vulkan layer hooks the loader itself, and TriggerCapture works identically.
- Module ABI: services table provides `IsRenderDocInjected` (`RenderModuleAPI.h:85`; engine impl `RendererModule.cpp:144-145`, load-time validated `:904`). The module-side forwarder `RenderDoc_IsInjected` lives in `RendererGLModule.cpp:132-134`, which **is compiled into renderer-vk** (not in `RENDERER_VK_EXCLUDED_SOURCES`, `tools\build\meson_sources.py:112-126`). Its only renderer consumer is the GL-failure path `R_ErrorForMissingRequiredOpenGLFeatures` (`RenderSystem_init.cpp:102-108`), which is GL-extension-check specific. **Conclusion: the vk module needs nothing new for Phase H.** `tools\debug\renderdoc_capture.ps1` already parameterizes `+set r_multiSamples` (line 82). (Speculation: optionally surface RenderDoc detection in vk gfxinfo, but nothing depends on it.)

## 5. Cinematic / ROQ full-screen paths — confirmed no H work
- The vk stage walk resolves cinematic stages exactly like GL: `pStage->texture.cinematic->ImageForTime` → `globalImages->cinematicImage->UploadScratch` (`vk_GuiExecutor.cpp:1740-1752`, mirroring `tr_render.cpp:568`). `UploadScratch` is a shared TU (`Image_load.cpp:1274-1311`) driving `AllocImage`/`SubImageUpload`, both real under vk (`vk_Image.cpp:348-419`, `:435-502`). Full-screen ROQ menus/videos and in-material cinematics render today; nothing Phase H-shaped remains here.